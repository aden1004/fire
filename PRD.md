# 소방설비기사(전기) 필기 기출 문제은행 PRD

> 단기 시험 대비용 개인 학습 웹앱. **OCR 텍스트(주)** + **원본 PDF Vision(검증·교정)** 이중 소스로 문제·보기·정답을 정확히 보존한다.

## 1. 배경 & 목표
- 이번 주 일요일 시험을 앞두고 단기간에 기출을 반복 학습할 수 있는 도구가 필요.
- 기존 OCR-only 문제은행은 인식 오류로 인해 **엉뚱한 답**을 표시하는 사고가 있었음.
- 책에 수록된 **니모닉/암기팁**은 단기 암기 효율을 크게 올려주므로, 문제와 함께 + 별도 모음으로 모두 노출.
- **핵심 원칙: 문제의 내용·번호·보기 순서·기호를 절대 변형하지 않는다.** 의역·요약·재작성 금지.

## 2. 사용자 & 사용 시나리오
1. 출근길/자투리 시간에 모바일로 과목·회차·중요도 필터를 걸어 빠르게 회독.
2. PC에서 모의고사 모드로 80문항(과목별 20×4) 시간 제한 풀이.
3. 틀린 문제는 자동으로 오답노트에 적재되어 시험 전날 집중 복습.
4. 책의 니모닉만 따로 모아 등하굣길에 훑어보기.

## 3. 범위
### In-Scope
- 4과목 × 회차별 기출문제(2025년 우선, 이후 다년치 확장).
- 문제·보기·정답·해설·암기팁·중요도(별1/별2/별3)·도식(이미지) 보존.
- 필터: 과목(4) / 회차 / 중요도 / (확장) 단원·키워드.
- 학습 모드: 일반 회독, 오답노트, 북마크, 랜덤 모의고사(시간 제한).
- 암기팁 전용 페이지(과목·키워드별 그룹화).
- 오프라인 PWA(설치 가능, 데이터 캐시).

### Out-of-Scope (이번 버전)
- 사용자 계정/소셜 로그인, 다중 사용자(개인용).
- 결제·구독.
- 자동 채점 답변 해설 LLM 생성(원본 해설만 사용).

## 4. 데이터 파이프라인 (Vision-Primary, 사용자 PC에서 실행)

> **전략 변경 사유**: 2025년 OCR 텍스트 실측 결과, 책의 2단 조판 때문에 좌·우 컬럼이 한 줄에 섞이는 케이스가 많아 OCR-only 파싱이 위험. 따라서 **Vision을 주 소스로 격상**하고 OCR은 페이지 매핑·기억법·별표 등의 **힌트**로만 사용한다. 큰 원본 PDF(≈150MB)는 사용자 PC에 있으므로 파이프라인은 로컬에서 실행되고, **결과 JSON만 깃에 커밋**한다.

```
원본 PDF (로컬)               OCR PDF→txt (로컬, 힌트)
   │                                  │
   ▼                                  ▼
 페이지 PNG 렌더            회차/과목/기억법/별표 힌트 추출
   │                                  │
   └─────────────┬────────────────────┘
                 ▼
         Claude Vision (페이지 PNG + OCR 힌트)
                 │
                 ▼
       페이지별 구조화 JSON (캐시: data/cache/<sha256>.json)
                 │
                 ▼
       merge & validate → data/normalized/2025.json (커밋 대상)
                 │
                 ▼
              Next.js 빌드
```

### 4.1 단계
1. **OCR 힌트 추출** (`scripts/extract_hints.py`)
   - 회차 헤더(`2025년 기사 제N회`) 위치 → 라인 구간 산출
   - 페이지 푸터(`25. 02. 시행/기사`) → 회차→페이지 추정
   - `기억법` 라인 + 전후 5줄 → 페이지/회차별 니모닉 후보 리스트
   - 별표류 문자(`★ ☆ 食 命 슈 黎`) 군집 → 페이지별 별 개수 카운트
   - 과목 헤더(`제 N 과목`) → 회차별 과목 시작 페이지 매핑
2. **페이지 렌더** (`scripts/render_pages.py`): `pypdfium2`로 원본 PDF 페이지를 200DPI PNG로 저장 (`data/cache/pages/<round>/p<NNN>.png`).
3. **Vision 추출** (`scripts/vision_extract.py`): 페이지 PNG + 해당 페이지의 OCR 힌트(니모닉·별표 카운트)를 Claude(`claude-opus-4-7`)에 전달, 페이지에 등장하는 문제들의 구조화 JSON을 받음. SHA256 캐시.
4. **병합·검증** (`scripts/build_dataset.py`)
   - 페이지별 결과를 회차·문제번호로 합쳐 240문항(3회차 × 80) 완성.
   - 자동 점검: 회차당 80문항·과목당 20문항·정답 라벨 ①~④·보기 4개·중요도 1~3.
   - 누락/이상치는 리포트로 출력하고 해당 페이지 재처리.
5. **빌드**: `data/normalized/2025.json`을 Next.js 정적 임포트(문항 수 ≤ 수천).

### 4.2 Vision 프롬프트 가드
- "페이지 이미지에 보이는 문항 텍스트·보기·정답 라벨을 한 글자도 변형·요약하지 말 것"을 시스템 프롬프트에 명시.
- 출력은 JSON Schema로 강제(보기 정확히 4개, label은 ①②③④ 중 하나).
- 기억법/별표/해설은 OCR 힌트와 교차 확인하되 **본문 텍스트는 이미지가 우선**.

## 5. 데이터 스키마

```json
{
  "id": "2025-01-1-12",
  "year": 2025,
  "round": 1,
  "subject": "소방전기일반",
  "subject_code": 2,
  "number": 12,
  "importance": 3,
  "stem": "원문 문제 텍스트 그대로",
  "choices": [
    {"label": "①", "text": "..."},
    {"label": "②", "text": "..."},
    {"label": "③", "text": "..."},
    {"label": "④", "text": "..."}
  ],
  "answer": "②",
  "explanation": "원본 해설 텍스트 그대로",
  "mnemonics": [
    {"keyword": "비상방송설비 음량조정기 배선", "tip": "3선식 — 공통/업/음량"}
  ],
  "figures": [
    {"page": 12, "bbox": [x,y,w,h], "asset": "/assets/2025-r1/p12-fig1.png"}
  ],
  "source": {
    "ocr_confidence": 0.92,
    "vision_verified": true,
    "vision_model": "claude-opus-4-7",
    "page": 12,
    "checksum": "sha256:..."
  },
  "tags": ["회로해석", "옴의법칙"]
}
```

- `subject_code`: 1=소방원론, 2=소방전기일반, 3=소방관계법규, 4=소방전기시설의 구조 및 원리
  *(실제 PDF 분석 후 정확한 과목명/순서로 보정)*
- `importance`: 별1=1, 별2=2, 별3=3.

## 6. 화면 설계 (Next.js App Router)
- `/` 홈: 최근 풀이, 통계 요약, 빠른 시작(과목·회차·중요도 칩).
- `/questions` 목록: 필터바(과목 / 회차 / 중요도 / 검색 / 오답·북마크 토글) + 무한 스크롤.
- `/questions/[id]` 단일 문제: 문제·보기·정답 토글·해설·니모닉 카드·도식 이미지.
- `/mnemonics` 암기팁 모음: 과목/키워드 트리, 검색.
- `/mock` 모의고사: 과목당 20문항 × 30분(총 2시간) 또는 사용자 설정. 종료 후 채점·오답 자동 적재.
- `/notes` 오답노트 & 북마크.
- `/settings` Vision 재실행 / 캐시 초기화 / 데이터 내보내기(JSON, Anki .apkg).

## 7. 정확성 가드 (핵심)
- LLM 호출 프롬프트에 **"문제·보기·정답 텍스트를 한 글자도 변경·요약·번역하지 말 것"** 명시.
- LLM 출력은 항상 JSON 스키마 검증 + **원문 OCR과의 Levenshtein 거리 리포트**(과도한 변경 시 사람 검수 큐로).
- 정답 라벨은 보기 라벨 중 하나여야 함(`①~④`).
- 빌드 산출물에 `provenance` 필드(어떤 모델·어떤 PDF 페이지에서 왔는지) 동봉.
- 사람이 신뢰 못 하는 항목 빠르게 발견하도록 `/questions/[id]`에 **원본 PDF 페이지 미리보기** 토글.

## 8. 비기능 요구
- 모바일 우선 반응형(Tailwind).
- PWA: `next-pwa`로 데이터·이미지 캐시, 오프라인 회독 가능.
- 빌드 산출물은 정적이므로 Vercel 무료 티어로 충분.
- 빌드 타임 Vision 호출은 변경된 페이지만 재처리(콘텐츠 해시 캐시).

## 9. 기술 스택
- Next.js 15 (App Router) + TypeScript + Tailwind.
- 데이터: 빌드 후 JSON 정적 임포트(문항 수 ≤ 수천이면 충분). 확장 시 SQLite + better-sqlite3.
- 전처리 스크립트: Python(`pdfplumber`, `pypdfium2`로 페이지 PNG화) + Node 전송 또는 전부 Node(`pdfjs-dist` + `sharp`). 1차는 Python.
- Vision: `@anthropic-ai/sdk`(Node) 또는 `anthropic`(Python).
- 상태/저장: 클라이언트는 `IndexedDB`(오답·북마크·진행률). 동기화 없음.

## 10. 디렉토리 구조 (계획)
```
/data
  /raw/2025/round1/source.pdf
  /raw/2025/round1/source.ocr.txt
  /cache/                # vision 결과 해시 캐시
  /normalized/2025-r1.json
/scripts
  parse_ocr.py
  vision_verify.py
  build_dataset.py
  validate.py
/app
  /(routes)/...
/public/assets/...
PRD.md
```

## 11. 마일스톤
| # | 산출물 | 완료조건 |
|---|---|---|
| M1 | 2025년 1회차 파싱 JSON | 자동 검증 통과(문항수·정답 분포) |
| M2 | Next.js 뷰어 MVP | 과목/회차/중요도 필터 + 단일 문제 보기 |
| M3 | 암기팁 페이지 + 오답노트 + 북마크 | IndexedDB 영속 |
| M4 | 모의고사 모드 + PWA | 시간 제한·채점·오프라인 동작 |
| M5 | 다년치 확장 | 폴더만 추가하면 빌드되는 구조 |

## 12. 리스크 & 대응
- **OCR 누락된 도식이 정답 단서**: Vision으로 페이지 PNG 보존 + `figures[]` 임베드.
- **Vision API 비용 폭주**: 트리거 규칙 + 해시 캐시 + 첫 빌드 후 변경 페이지만 재호출.
- **원문 훼손 가능성**: §7 가드 + 빌드 리포트에 변경 라인 diff 표시, 의심 항목은 표시한 상태로 노출.
- **시간 부족**: 1회차 → 전회차 순으로 단계 배포. MVP가 항상 가용.

## 13. 채택된 결정
- Vision 모델: **Claude API (Anthropic)**
- 배포: **Next.js (로컬 + Vercel)**
- Vision 실행 시점: **빌드 타임 사전 처리(캐시)**
- 추가 기능: **오답노트/북마크, 랜덤 모의고사, PWA**

## 14. 추후 아이디어 (백로그)
- SRS(간격 반복) 기반 복습 큐.
- 키워드 검색·태그 자동 생성(LLM 분류).
- 학습 진도 통계(과목별 정답률, 취약 단원).
- Anki 덱 export.
